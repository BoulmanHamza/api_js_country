class University {
    constructor(country, name, domains, alpha_two_code, web_page) {
        this.country = country
        this.name = name
        this.domains = domains
        this.alpha_two_code = alpha_two_code
        this.web_page = web_page
    }
}

export default new University()